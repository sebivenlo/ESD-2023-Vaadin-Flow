name: Docker Image CI

on:
  pull_request:
    branches: [ "main" ]

jobs:

  publish-docker-image:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build /vaadin-app --file Dockerfile --esde-vaadin:latest
